# Package dependencies to install
pnpm add mongodb mongoose zod @types/node
pnpm add -D @types/mongodb

# Additional packages for dashboard features
pnpm add react-hook-form @hookform/resolvers
pnpm add react-dropzone # for file uploads
pnpm add date-fns # for date handling
pnpm add react-hot-toast # for notifications
